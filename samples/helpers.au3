; Stub include referenced by hello.au3. The main sample doesn't actually
; call anything from here — the `#include "helpers.au3"` line is there to
; demonstrate the quoted-include directive form. AutoIt3 and Au3Check
; both fail at parse time if the include can't be resolved, so this file
; exists as an empty stub.
